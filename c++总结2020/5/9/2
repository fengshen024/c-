1、首先二分法，我们接触了两种，一种定义了l,r然后向中间查找是不是满足条件，第二个是所谓的标尺法（也叫量尺法） 定义了L和R ，都是他们为0，然后用数组
分别表示出那个长度用l-r来表示截止条件是r>num，算法复杂度为n，
2、dfs：
1--dfs里面存在很多减支，想办法表示出来，确定一个终止条件是dfs的灵魂，构建出dfs的框架，可以用数组来存下哪一个用过的，


3、等差数列的运用，不要忘记注意范围，可以考虑先除一遍然后在×一遍看是不是满足条件
